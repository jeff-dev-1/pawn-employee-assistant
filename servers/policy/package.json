{
  "name": "@pawn/policy-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=../../.env src/server.ts",
    "start": "tsx --env-file-if-exists=../../.env src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "bin": { "pawn-policy-mcp": "src/stdio.ts" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@pawn/mcp-kit": "*",
    "zod": "^4.5.4"
  },
  "devDependencies": { "@types/node": "^20.19.9", "vitest": "^3.2.4" }
}
